import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 30,
        
        backgroundColor: '#fff',
    },
    label: {
        fontSize: 18,
        fontWeight: 'bold',
        marginTop: 10,
    },
    info: {
        fontSize: 16,
        marginTop: 5,
    },
    materialsContainer: {
        marginTop: 10,
    },
    material: {
        fontSize: 16,
        marginTop: 5,
        borderWidth: 1,
        borderColor: '#ccc',
        padding: 8,
        borderRadius: 5,
    },
});

export default styles;
